# 制作和分享自己的系列皮肤

更新日期：2026-09-10。格式版本为 1，支持文件夹制作及 ZIP 导入。

## 1. 准备素材

- 六张 PNG：L1～L6。
- 一张 PNG：EX 视频封面。
- 一段 MP4：EX 视频，建议使用 H.264 编码。

六张主图建议同尺寸、16:9，主体位置尽量连续。封面建议取自 EX 视频首帧，减少封面转视频时的构图跳变。视频本身尽量避免大幅镜头移动，循环衔接效果要在实际窗口中确认。

## 2. 建立文件夹

从完整工具包复制 `skins/example` 整个文件夹，给副本起名，例如 `我的机甲`。不要直接编辑唯一一份默认示例。

将素材放到副本中，保持以下结构：

```text
我的机甲/
  skin.json
  l1.png
  l2.png
  l3.png
  l4.png
  l5.png
  l6.png
  ex.png
  ex.mp4
```

## 3. 编辑 skin.json

用记事本打开配置，按以下格式修改，并以 UTF-8 保存。确认扩展名是 `.json`，不是 `.json.txt`。

```json
{
  "formatVersion": 1,
  "name": "我的机甲",
  "author": "你的名字",
  "levels": ["l1.png", "l2.png", "l3.png", "l4.png", "l5.png", "l6.png"],
  "ex": {
    "poster": "ex.png",
    "video": "ex.mp4"
  }
}
```

`levels` 的排列决定画面顺序。`name` 是面板里显示的名称，也是导入时用来生成文件夹名的依据；更新作品时可用“我的机甲 v2”避免同名冲突。不要使用路径分隔符或特殊路径名称。

## 4. 压缩、导入和验证

1. 右键你的皮肤文件夹，选择 **压缩为 ZIP 文件**，或 **发送到 → 压缩(zipped)文件夹**。
2. 打开换肤面板，先结束正在运行的换肤会话。
3. 点击 **导入皮肤 ZIP**，选择刚做好的 ZIP。
4. 选择新皮肤，依次检查 L1～L6 和 EX 封面。
5. 启动换肤，测试原生滑块、EX 视频、返回主线和结束换肤。
6. 确认没有缺图、无法播放或明显裁切后，再分享 ZIP。

ZIP 内只能有一个 `skin.json`。素材直接放在配置同级目录，不要把多套皮肤一起压成一个导入包。

## 5. 格式限制

| 项目 | 限制 |
|---|---|
| 格式版本 | formatVersion 必须为 1 |
| 名称 | 非空，最多 80 个字符 |
| 作者 | 可省略，最多 80 个字符 |
| 主图 | 恰好六项，PNG |
| EX | PNG 封面和 MP4 视频 |
| 单张 PNG | 不超过 20 MiB，宽高均不超过 8192，总像素不超过 33,554,432 |
| MP4 | 不超过 80 MiB |
| 素材及配置累计读取量 | 不超过 150 MiB |
| 素材位置 | 与 skin.json 同级，只允许本地文件名，不允许网络地址或目录穿越 |

文件头检查通过不等于所有视频编码均可播放，需要实际测试。工具不执行皮肤内自定义 JavaScript、HTML 或 CSS；本格式也没有调色、字体和转场参数。

## 6. 机甲系列示例与分享

本次机甲系列将作者命名的 `1.png`～`6.png` 依次打包为 `l1.png`～`l6.png`，`EX.mp4` 打包为 `ex.mp4`，并从视频首帧提取 `ex.png`。原六张图片和视频未重新压缩。

给朋友分享皮肤 ZIP 时，同时附上 [完整工具下载页](https://github.com/max960957-bit/codex-slide-evolution-skin/releases) 和 [使用手册](使用说明.md)。对方需要先安装工具，再导入皮肤。

仅分享自己有权分发的素材。代码的 MIT 许可证不自动替代图片和视频各自的授权条件。

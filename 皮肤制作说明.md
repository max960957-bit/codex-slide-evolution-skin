# 制作自己的皮肤

这是自定义皮肤格式第一版。当前先使用文件夹；最新便携包已自带 Node 运行环境，ZIP 导入和安装器尚未交付。

1. 复制 `skins/example` 整个文件夹，给副本改名。
2. 替换副本中的 `l1.png` 至 `l6.png`，对应原生滑块从左到右的六张背景。
3. 替换 `ex.png` 封面和 `ex.mp4` 视频，对应闪电／2× 模式。
4. 用记事本打开 `skin.json`，修改 `name` 和 `author`，保留文件名及其他结构。
5. 保存工作、退出普通 Codex，双击工具目录的 **启动自定义皮肤.cmd**，选择副本中的 `skin.json`。

返回默认皮肤：结束换肤，再使用原来的 **启动换肤.lnk**。自定义素材不会覆盖默认素材，也不修改 Codex 安装文件。

图片建议使用同尺寸、相近人物位置的 16:9 PNG；视频建议使用浏览器可播放的 H.264 MP4。播放器会铺满窗口并裁切，非横屏素材可能裁掉较多内容。EX 视频和封面的构图尽量保持一致，减少入场差异。

格式限制：恰好六张 PNG、一个 PNG 封面和一个 MP4。文件必须直接放在 skin.json 同一文件夹，不能引用网络地址或外部路径。每张 PNG 不超过 20 MB，最大边长 8192、总像素不超过 33554432；MP4 不超过 80 MB，总读取量不超过 150 MB。文件头校验不保证所有编码都能播放，损坏素材仍可能在加载时被拒绝。

皮肤只包含素材和 JSON 配置，不运行自定义脚本。动画、滑块逻辑和转场由工具统一管理；本版尚不开放调色、字体与动画曲线自定义。

分享自己制作的皮肤时，分享整个文件夹即可；请只分发你有权分享的素材。随附示例当前用于本机测试，默认素材的公开分发许可尚待确认。

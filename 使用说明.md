# Codex 滑动变富器 · 本机试用版

六张背景随滑块即时切换，EX 播放动态背景。聊天保持原生位置，已接入的助手回复直接显示在背景上。当前视觉方向已确认，后续以稳定性收尾为主。

## 启动与退出

1. 保存工作，完全退出普通 Codex。
2. 在此文件夹双击 **启动换肤.cmd**；它调用既有 run-d6-sequence.cmd。本机旧的启动换肤快捷方式也可继续用。
3. 等待背景和右上角控件出现，期间保持启动器窗口打开。
4. 打开输入框旁的原生模型/推理强度菜单，显示 Power 滑块。首次识别后，右上角重复的六档按钮和滑块会隐藏；拖动原生滑块时背景跟随变化。原生模型/推理设置仍照常生效，换肤程序只读取控件的档位，不更改设置。
5. 使用完点击右上角 **结束换肤**。等待清理并恢复普通 Codex，然后按启动器提示关闭它。

更新代码后，需要结束旧换肤会话再重新启动。不要在浏览器地址栏打开 cmd 文件。移动便携版时保留整个文件夹，旧 .lnk 快捷方式不能跨电脑使用。

最新便携测试包需要 Windows x64 和官方 Codex 26.901.6511.0。已内置 Node.js 24.16.0，不需要另外安装 Node.js；请完整解压，保留 vendor/node 文件夹。换肤入口使用随附 visual-core，不需要原项目、Git 或开发工具；原 D4/D5 诊断脚本分支仍保留开发机基线检查。它仍是解压使用的测试包，不是带安装卸载向导的正式安装器。

当前已加入当前用户安装测试：双击 **install-user.cmd** 会复制到 `%LOCALAPPDATA%\CodexSlideEvolutionSkin`，并创建开始菜单快捷方式；**uninstall-user.cmd** 删除该安装目录和快捷方式。安装不需要管理员权限，也不触碰普通 Codex 安装。实际分发前仍需在干净电脑验证。

升级会保留 `skins` 下除 `example` 外的用户皮肤；新增的默认示例会更新。安装后双击 **管理皮肤.cmd**，可以查看已安装皮肤、导入 ZIP，并启动选中的皮肤。也可以单独双击 **导入皮肤.cmd**。导入会验证唯一 skin.json、路径和素材格式，重名会拒绝覆盖。

自定义皮肤请阅读 [皮肤制作说明](皮肤制作说明.md)，通过 **启动自定义皮肤.cmd** 选择 skin.json。第一版支持皮肤文件夹，尚未提供 ZIP 导入或皮肤商店。

原生菜单六档时逐档对应 L1–L6；档位数量不同时按从左至右的位置映射六张背景，较少档位会跳过部分图片。关闭菜单保留当前背景；再次打开时重新读取当前档位。识别前仍保留独立控件供回退。闪电／2× 快速模式开启时进入 EX，关闭时返回当前原生滑块对应的背景；开启期间拖动滑块保持 EX，关闭后使用最新档位。快速模式识别成功后，独立 EX 和返回按钮隐藏，只保留结束换肤。换肤只跟随快速模式状态，它原本的服务设置仍照常生效。锁定或无法确认的控件不联动。

2026-09-08 用户已确认在 Codex 26.901.6511.0 上启动、原生滑块换图、闪电／2× 切换 EX 均正常。首次启动需打开一次该原生菜单，菜单未挂载时不会读取或猜测已保存设置。

## 遇到问题

- 没有启动成功或窗口意外关闭：查看本目录 runs 中最新文件夹内的 D6_RESULT.md，将报告路径或报错发给我。不要连续启动多个实验窗口。
- PARTIAL：部分检查未覆盖。若 FailureReason 为 NONE、恢复检查为 PASS，就不等于运行失败；无需只为消除这个标签反复重跑。
- FAILED 或恢复检查 FAIL：保留报告，先排查该次失败。
- 更新 Codex 后被版本检查拦住：需要重新核对兼容性，不要跳过检查。

## 当前状态

已验证：本地六档、EX 往返、长内容滚动、草稿保留、任务节点替换与样式恢复。2026-09-08 的 095519 和 100459 两次实机报告均记录原生回复 APPLIED、清理与普通 Codex 恢复 PASS、FailureReason NONE。

新版 134443 运行报告记录 EX 实际播放、返回主线、显式结束及全部清理恢复 PASS，FailureReason NONE；结束时没有可见最终回复，且未访问 L4，保留 PARTIAL。较早的 133739 轮存在 L1 注入通信超时及页面清理失败，最终进程/端口/profile 清理和普通 Codex 恢复通过，原因尚未确认。因此尚不宣称启动和长期运行完全稳定。

新增本地回复完成接续检查：模拟分段更新、最终回复标记出现、旧回复样式恢复，输入草稿及焦点均保留；未发送真实消息。正在生成且没有最终回复标记的块保持原生样式。

仍待验证：真实新回复生成、任意页面导航、长时间运行与客户端更新后的兼容性。无气泡处理当前只跟随一个受支持的可见最终助手回复，未覆盖所有消息类型。本版不是独立安装包，也不提供开机自启。

启动时暂时禁用本次实验的 GPU 加速，普通 Codex 恢复不附加该参数。全程不保存对话截图，不读取对话正文或草稿。原图和视频未改，EX 原视频为 720p。

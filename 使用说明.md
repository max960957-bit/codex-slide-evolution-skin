# Codex 换肤详细使用手册

更新日期：2026-09-10。适用于当前仓库代码；下载包的功能以该版本发布说明为准。

## 一、先确认你下载的是什么

| 文件 | 用途 | 怎么用 |
|---|---|---|
| 完整工具 ZIP，例如 `codex-slide-evolution-skin-v0.1.4.zip` | 安装换肤面板和运行程序 | 解压后双击“启动换肤” |
| 独立皮肤 ZIP，例如 `Codex皮肤-机甲-v1.0.zip` | 给已安装的工具增加一套素材 | 在面板点击“导入皮肤 ZIP” |
| GitHub 自动生成的 `Source code` | 开发源码快照 | 初次使用请优先选择发布者上传的完整工具包 |

工具要求 Windows 11 x64，以及官方 Store Codex `26.901.6511.0`。当前代码会拒绝其他 Codex 版本。工具不包含 Codex 客户端或会员额度。

## 二、第一次安装

1. 打开 [下载页](https://github.com/max960957-bit/codex-slide-evolution-skin/releases)。
2. 展开要使用版本的 **Assets／资产**。
3. 点击发布者上传的完整工具 ZIP，等待下载结束。
4. 在文件资源管理器找到下载的 ZIP，右键选择 **全部解压缩**。
5. 打开解压后的文件夹；如果里面还有一层文件夹，继续打开，直到看见“启动换肤”。
6. 双击 **启动换肤.cmd**。Windows 隐藏扩展名时，它显示为“启动换肤”，类型为“Windows 命令脚本”。不要在浏览器地址栏输入这个文件路径。
7. 等待自动安装和面板打开。首次安装会创建桌面 **Codex 换肤** 快捷方式。

必须完整解压，不能只拖出一个启动脚本，也不能直接在 ZIP 预览里运行。

若看到 Windows 安全提示，请先核对文件来自本项目发布页；不需要为了运行工具而关闭系统防护。程序没有可供保证信任的正式安装器签名。

## 三、启动一套皮肤

1. 在面板 **皮肤库** 左侧选择一套皮肤。默认示例可能显示为 **My Skin**。
2. 点击右侧 **L1～L6** 查看阶段图；点击 **EX** 查看视频封面。
3. 点击右下角 **启动换肤**。
4. 如果提示“请保存工作并退出普通 Codex”，先保存正在编辑的内容并等待任务结束，再关闭普通 Codex 窗口。保持换肤面板打开。
5. 面板检测到普通 Codex 已退出后，会自动继续启动，无需反复点击。
6. 等待 Codex 窗口及皮肤素材加载完成。首次加载可能需要一些时间。

换肤期间保持控制面板运行。不要通过任务管理器结束它来停止换肤。

## 四、聊天时如何切换画面

1. 在 Codex 输入区域找到模型／推理强度入口，打开原生滑块菜单。
2. 拖动滑块，背景随可识别的档位变化。六张图片对应工具的 L1～L6；原生控件位置少于六档时，不一定能逐一显示全部六张。
3. 点击原生 **闪电／2×** 控件启用对应模式时，工具尝试进入 EX 视频。
4. 关闭该模式后再调整原生滑块，可回到主线背景。如果换肤窗口右上角仍显示 **返回**，也可以直接点击它。

原生控件仍然会改变 Codex 自身的推理档位或快速模式。换肤只是跟随这些操作；请按实际任务选择模型设置。

面板上的 L1～L6／EX 按钮用于静态预览，不是远程控制已打开聊天窗口的开关。EX 预览不动属于正常情况。

## 五、结束换肤、换另一套皮肤

1. 回到控制面板。
2. 点击 **结束 / 取消等待**。如果仍在等待普通 Codex 退出，这个按钮只取消等待；如果已启动，它会请求结束本次换肤。
3. 等待清理完成。素材加载或转场进行中时，结束可能不是立即完成。
4. 程序尝试恢复普通 Codex；面板恢复可选择状态后，可以关闭面板。
5. 想换另一套：选择新的皮肤，再按“启动一套皮肤”的步骤启动。

以后日常使用，双击桌面 **Codex 换肤** 快捷方式即可，不必重复下载或解压。

## 六、导入第二套皮肤，以“机甲”为例

1. 准备好 `Codex皮肤-机甲-v1.0.zip`。如果皮肤作者单独提供 ZIP，它不需要提前解压。
2. 打开桌面 **Codex 换肤**。
3. 如有正在运行的换肤会话，先结束并等待面板恢复可操作状态。
4. 在 **皮肤库** 点击 **＋ 导入皮肤 ZIP**。
5. 选择该 ZIP 并打开。
6. 看到“导入完成”后，在左侧选择 **机甲**。
7. 预览 L1～L6 和 EX，然后点击 **启动换肤**。

若有人已帮你在这台电脑导入，重新打开面板直接选“机甲”，不必再次导入。提示同名皮肤已存在时，先使用现有版本；更新自制皮肤的方法见 [皮肤制作说明](皮肤制作说明.md)。

## 七、升级工具

GitHub 源码更新不会自动更新你电脑里的程序，也不会自动替换旧 Release 附件。

1. 先结束换肤，关闭控制面板。
2. 按下一节方法备份 `skins` 文件夹，尤其是自己改过的 `example`。
3. 从 Releases 下载明确包含所需修复的新完整工具包。
4. 解压到一个新的文件夹。
5. 双击新文件夹中的 **启动换肤.cmd**，将新程序安装到用户目录。
6. 安装后重新选择皮肤。

当前安装逻辑保留 `skins` 中其他命名的皮肤文件夹，但会更新默认 `example`。因此不要只在 `example` 里保留唯一一份个人作品。

2026-09-09 提交的断线恢复修复位于源码中；v0.1.4 完整包已包含该修复，之前的 v0.1.3 ZIP 不包含。不要仅凭仓库首页有新提交就判断下载包已升级。

## 八、备份与卸载

### 备份皮肤

1. 结束换肤并关闭面板。
2. 按 **Win + R**。
3. 粘贴 `%LOCALAPPDATA%\CodexSlideEvolutionSkin\skins`，按回车。
4. 把需要保留的整个皮肤文件夹复制到另外的位置，或保留原始皮肤 ZIP。

### 卸载

1. 完成上述备份。
2. 打开面板，进入 **设置**。
3. 点击 **卸载换肤工具…**。
4. 确认提示后，程序会尝试删除工具安装目录，包括已导入皮肤。
5. 当前卸载脚本不会主动删除桌面和开始菜单快捷方式；卸载后可手动删除失效的 **Codex 换肤**／**Codex 滑动变富器** 快捷方式。

卸载不卸载 Codex 本体。若提示文件正在使用，结束换肤并关闭相关窗口后再尝试；不要对 Codex 自身目录进行清理。

## 九、常见问题

| 现象 | 具体处理 |
|---|---|
| 提示当前 Codex 版本不支持 | 记下提示中的版本，通过 Issues 反馈；等待对应适配，不手工跳过版本校验 |
| 一直提示退出普通 Codex | 保存内容，关闭所有普通 Codex 窗口，保持面板打开并等待几秒 |
| 提示文件缺失 | 重新完整解压工具 ZIP，再从解压目录运行“启动换肤” |
| 面板 EX 预览不动 | 面板只显示 EX 封面；视频在启动后的 Codex 换肤窗口播放 |
| 使用一段时间后背景消失 | 含恢复修复的版本会尝试重连；若未恢复，保存工作后重新启动换肤，并保留最新运行报告 |
| 背景模糊 | 先区分面板缩略图和聊天窗口原图；换更高分辨率素材，视频画质不能靠放大改善 |
| 人物被裁切或文字不清楚 | 尝试调整窗口大小、使用深色模式；制作皮肤时把重点主体避开聊天正文区域 |
| ZIP 导入失败 | 确认只有一个 skin.json、六张 PNG、EX PNG 和 MP4，且文件名与配置一致 |
| 提示同名皮肤已存在 | 不支持同名覆盖；修改新皮肤 skin.json 的 name 后重新打包 |
| 旧脚本出现黑窗口 | 首次 CMD 入口仍可能出现控制台；平时使用安装生成的桌面快捷方式。持续残留时反馈所用入口 |
| 高级面板没有显示 | 查看基础面板错误信息，检查 WebView2 Runtime 是否可用；基础面板仍提供选择、导入、启动等操作 |

## 十、如何提交有效反馈

1. 打开面板底部 **运行详情与错误信息**，记录可见错误；该区域没有内容时继续下一步。
2. 按 **Win + R**，输入 `%LOCALAPPDATA%\CodexSlideEvolutionSkin\runs`。
3. 按修改时间找到最近的运行文件夹，打开 `D6_RESULT.md`；有 `recovery.log` 时也可以附上。
4. 在 [Issues](https://github.com/max960957-bit/codex-slide-evolution-skin/issues) 填写下面的信息。

```text
工具包版本／下载的 ZIP 文件名：
Codex 版本（运行报告中的 CodexVersion）：
Windows 版本：
使用的皮肤：
从哪个入口启动：
做了什么之后出问题：
大约运行多久：
期间是否休眠、更新、重开窗口：
报错文字／运行报告：
```

分享前检查并遮盖姓名、本机路径等个人信息；不需要上传聊天正文、账号凭据或整个运行目录。
